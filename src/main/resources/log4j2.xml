<?xml version="1.0" encoding="UTF-8"?>
<Configuration shutdownHook="disable">
  <Properties>
    <Property name="log.pattern">%d{yyyy-MM-dd HH:mm:ss} [%t] %-5level %logger{36} - %msg%n</Property>
    <Property name="console.pattern">%d{yyyy-MM-dd HH:mm:ss} %logger{36} - %msg%n</Property>

  </Properties>
  <Appenders>
    <!-- to console -->
    <Console name="console" target="SYSTEM_OUT">
      <PatternLayout pattern="${console.pattern}" />
    </Console>
    <!-- to file -->
    <RollingFile name="logFile" fileName="logs/jload.log" filePattern="logs/jload-%d{yyyy-MM-dd-HH-mm-ss}.log">
      <PatternLayout>
        <Pattern>${log.pattern}</Pattern>
      </PatternLayout>
      <Policies>
        <TimeBasedTriggeringPolicy />
      </Policies>
    </RollingFile>

  </Appenders>
  <Loggers>
    <Root level="debug">
      <!-- info level -->
      <AppenderRef ref="console" level="info" />
      <!-- debug level -->
      <AppenderRef ref="logFile" level="debug" />
    </Root>
  </Loggers>
</Configuration>